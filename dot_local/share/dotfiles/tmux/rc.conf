# plugins

# TPM

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

set -g @plugin 'tmux-plugins/tmux-yank'

#

# True Color Support
set -sa terminal-overrides ",*-256color:Tc"

set -g mouse on

set -g display-panes-time 10000

set -g base-index 1
setw -g pane-base-index 1

#

set -g status-position top

set -g status-left ' #{=/20/…:session_name} '
set -g status-left-length 23 # status-left に依存 (' ' (1) + '#{session_name}' (20) + '…' (1) + ' ' (1))
set -g status-left-style 'bg=default fg=cyan bold'

set -g status-right ' #{?pane_in_mode,COPY,}#{?client_prefix,PREFIX,}#{?window_zoomed_flag,ZOOM,} '
set -g status-right-length 8 # status-right に依存 (' ' (1) + 'PREFIX' (6) + ' ' (1))
set -g status-right-style 'bg=default fg=cyan bold'

set -g status-style 'bg=default fg=default'

set -g window-status-format '#I:#W'
set -g window-status-style 'bg=default fg=default'
set -g window-status-current-format '#I:#W'
set -g window-status-current-style 'bg=default fg=cyan bold'

set -g pane-border-style 'bg=default fg=default'
set -g pane-active-border-style 'bg=default fg=cyan'

# Binds

bind Space display-panes

# Pane

bind h split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"

bind -n C-M-Left select-pane -L
bind -n C-M-Right select-pane -R
bind -n C-M-Up select-pane -U
bind -n C-M-Down select-pane -D

bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Down resize-pane -D 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Right resize-pane -R 5

# Window

bind c new-window -c "#{pane_current_path}"

bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

bind -n M-Left select-window -t -1
bind -n M-Right select-window -t +1
bind -n M-S-Left swap-window -t -1 \; select-window -t -1
bind -n M-S-Right swap-window -t +1 \; select-window -t +1

# Session

bind C new-session -c "#{pane_current_path}"

# TPM

run '~/.tmux/plugins/tpm/tpm'

# vim: set ft=tmux:
