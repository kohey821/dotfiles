{{ if eq .chezmoi.os "darwin" -}}
# brew

eval (/opt/homebrew/bin/brew shellenv)
{{- end }}

# asdf

{{ if eq .chezmoi.os "darwin" -}}
source (brew --prefix asdf)/libexec/asdf.fish
{{- end }}

# bash_aliases

source ~/.bash_aliases

# envs

set -xg EDITOR nvim

